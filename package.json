{
  "name": "qr-attendance",
  "version": "1.0.0",
  "description": "A QR code based attendance system built with React, Node, Express and SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "dev": "node --watch server.js",
    "dev:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "cookie-session": "^2.1.0",
    "express": "^4.21.1",
    "qrcode": "^1.5.4"
  }
}
